# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*

# next.js
.next/
out/
build/
dist/

# production
*.tsbuildinfo
next-env.d.ts

# environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# prisma
prisma/*.db
prisma/*.db-journal
prisma/migrations/dev/

# logs & debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
logs/
*.log

# editors & os
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db
*.swp

# testing / coverage
coverage/
.nyc_output/

# uploads written by the quote form in local development
uploads/
public/uploads/

# large binary media dropped in by the marketing team
public/media/*.mp4
public/media/*.webm
public/media/*.mov
!public/media/.gitkeep
!public/media/README.md
# The hero background is the exception, and it has to be.
#
# Railway clones the repository fresh on every deploy — there is no persistent
# disk and no upload step — so a file that is only on somebody's laptop is a file
# production never sees. Ignored, the homepage would quietly fall back to the
# vector city scene on the live site while looking correct locally, which is the
# worst kind of difference between the two.
#
# Kept under 4 MB for that reason. Anything genuinely large belongs on a CDN with
# NEXT_PUBLIC_HERO_VIDEO_URL pointing at it — see public/media/README.md.
!public/media/hero.mp4
# No WebM alongside it any more. On the first clip VP9 and H.264 landed within
# 30 KB of each other, so shipping both cost nothing and covered Chromium built
# from source and Firefox on Linux, neither of which can decode H.264. This
# footage compresses very differently: 9.9 MB in VP9 against 4.7 in H.264, for
# the same picture. Doubling the payload for that audience is the wrong trade,
# and `resolveHeroVideo` already falls back to the vector scene for them.
# !public/media/hero.webm

# misc
.vercel
.turbo
public/downloads/*.pdf
public/media/scenes/
